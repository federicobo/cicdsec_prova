name: Images scanner and assesment

on:
  workflow_dispatch:


jobs:
  image-security-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        id: docker-login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - run: |
          pwd  
          ls
          echo ${{ github.workspace }}

      - name: Build DVNA Docker image
        id: docker-dvna-build
        uses: docker/build-push-action@v5
        with:
          context: "vuln_apps/dvna"
          push: false
          tags: dvna:latest

      - name: Docker Scout CVEs
        uses: docker/scout-action@v1
        with:
          command: cves
          image: local://dvna:latest
          sarif-file: dvna-cves.json
      # - name: Upload CVEs result
      #   id: upload-cves
      #   uses: github/codeql-action/upload-sarif@v2
      #   with:
      #     sarif_file: image-cves.json
