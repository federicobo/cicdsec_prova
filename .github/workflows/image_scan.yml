name: Images scanner and assesment

on:
  workflow_dispatch:

jobs:
  image-security-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Setup Docker buildx
        id: docker-build
        uses: docker/setup-buildx-action@2.5.0

      - name: Login to Docker Hub
        id: docker-login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build DVWA Docker image
        id: docker-dvwa-build
        uses: docker/build-push-action@v5
        with:
          context: vuln_apps/dvna/
          push: false
          tags: dvwa:latest

      - name: Docker Scout CVEs
        uses: docker/scout-action@v1
        with:
          command: cves
          image: local://dvwa:latest
          sarif-file: dvwa-cves.json
      # - name: Upload CVEs result
      #   id: upload-cves
      #   uses: github/codeql-action/upload-sarif@v2
      #   with:
      #     sarif_file: image-cves.json
